# secrets — never commit
mp_api_key.txt
*.key

# OBELiX data — auto-downloaded on first run, not redistributed (respects its license).
# Keep our own reproducible metrics.json tracked (mirrors Project 2's data/metrics.json).
data/*
!data/metrics.json

# trained model weight — regenerated by 01_train_eval.py (mirrors Project 2's *.model/*.pth)
*.cbm
# CatBoost training-log dir (written on every fit)
catboost_info/

# screen outputs (regenerated by 04_screen_mp.py)
screen_demo.csv
screen_mp_results.csv

# Python
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db
