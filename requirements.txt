# Compatibility install. The authoritative dependency declaration is pyproject.toml;
# exact versions used for the frozen release are in environment.lock.yml.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
catboost>=1.2
pymatgen>=2024.1
matminer>=0.9
matplotlib>=3.7
pillow>=9
mp-api>=0.40
